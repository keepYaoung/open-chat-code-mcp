tunnel: REPLACE_WITH_TUNNEL_ID
credentials-file: /Users/REPLACE_WITH_YOUR_USERNAME/.cloudflared/REPLACE_WITH_TUNNEL_ID.json

ingress:
  - hostname: mcp.example.com
    service: http://127.0.0.1:3000
  - service: http_status:404
